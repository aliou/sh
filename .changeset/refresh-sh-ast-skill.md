---
"@aliou/sh": patch
---

Refresh the bundled sh-ast skill: document source positions, `recoverErrors`, lazy `parseStmtsSeq`/`parseWordsSeq`, `splitBraces`/`BraceExp`, `ExtGlob`, and the select/coproc/time clauses. Drop stale limitations.
